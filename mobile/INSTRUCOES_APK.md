# 📦 Como Gerar o APK do MarmitaWare

**Nota:** Certifique-se de ter feito login no Expo antes (`eas login`) e de ter configurado o projeto (`eas build:configure`).

Execute o seguinte comando na pasta `./mobile/`:

```bash
cd mobile
eas build -p android --profile preview
```